<footer class="global-footer">
  <div class="under-footer">
    <p class="copyright">&copy; SUZUKI SOSAKU 2023</p>
  </div>
</footer>

<style lang="scss">
  @import '../styles/breakpoint.scss';

  .global-footer {
    background-color: #f8f8f8;
    position: sticky;
    top: 100vh;
  }

  .nav {
    @media (width > $breakpoint-md) {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 32px;
    }
  }

  .nav-items {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    @media (width > $breakpoint-md) {
      flex-direction: row;
      justify-content: center;
      gap: 32px;
    }
  }

  .nav-item {
    width: 100%;
    font-size: var(--fs-md);
    @media (width > $breakpoint-md) {
      width: auto;
    }
  }

  .nav-item__link {
    display: block;
    padding: 8px 0;
    text-align: center;
    @media (width > $breakpoint-md) {
      padding: 8px 16px;
    }
  }

  .under-footer {
    background: var(--bg-black);
    color: var(--text-white);
    text-align: center;
    padding: 16px;
  }

  .copyright {
    font-size: var(--fs-xs);
  }
</style>
